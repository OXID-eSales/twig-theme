<li>
    <a href="{{ seo_url({ ident: oViewConf.getSelfLink()|cat("cl=basket") }) }}" rel="nofollow">
        <i class="fa fa-shopping-cart"></i>
        {% if isset(oxcmp_basket) and oxcmp_basket.getItemsCount() > 0 %}
            <span class="badge">
                {{  oxcmp_basket.getItemsCount()  }}
            </span>
        {% endif %}
    </a>
</li>
